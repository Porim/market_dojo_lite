<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Market Dojo Lite" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "chartkick", "Chart.bundle" %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm border-b">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "Market Dojo Lite", root_path, class: "text-xl font-bold text-gray-900" %>
            </div>
            <% if user_signed_in? %>
              <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                <%= link_to t('navigation.dashboard'), root_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900" %>
                <%= link_to t('navigation.rfqs'), rfqs_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
                <%= link_to t('navigation.auctions'), auctions_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
                <% if current_user.buyer? %>
                  <%= link_to t('navigation.analytics'), analytics_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
                  <%= link_to "Reports", reports_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
                <% end %>
                <%= link_to t('navigation.api_docs'), api_docs_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
              </div>
            <% end %>
          </div>
          <div class="flex items-center">
            <!-- Language Switcher -->
            <div class="mr-4">
              <select onchange="switchLanguage(this.value)" class="text-sm border-gray-300 rounded-md">
                <option value="en" <%= 'selected' if I18n.locale == :en %>>English</option>
                <option value="es" <%= 'selected' if I18n.locale == :es %>>Español</option>
                <option value="fr" <%= 'selected' if I18n.locale == :fr %>>Français</option>
                <option value="de" <%= 'selected' if I18n.locale == :de %>>Deutsch</option>
              </select>
            </div>
            
            <% if user_signed_in? %>
              <span class="text-sm text-gray-500 mr-4">
                <%= current_user.name %> (<%= t("roles.#{current_user.role}") %>)
              </span>
              <%= link_to t('navigation.settings'), edit_user_registration_path, class: "text-sm text-gray-500 hover:text-gray-900 mr-4" %>
              <%= button_to t('navigation.logout'), destroy_user_session_path, method: :delete, class: "text-sm text-gray-500 hover:text-gray-900" %>
            <% else %>
              <%= link_to t('navigation.login'), new_user_session_path, class: "text-sm text-gray-500 hover:text-gray-900 mr-4" %>
              <%= link_to t('navigation.sign_up'), new_user_registration_path, class: "text-sm bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice.present? %>
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3" role="alert">
        <p><%= notice %></p>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3" role="alert">
        <p><%= alert %></p>
      </div>
    <% end %>

    <main class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
    
    <script>
      function switchLanguage(locale) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '<%= switch_locale_path(locale: 'LOCALE') %>'.replace('LOCALE', locale);
        
        const csrfToken = document.querySelector('meta[name="csrf-token"]').content;
        const csrfInput = document.createElement('input');
        csrfInput.type = 'hidden';
        csrfInput.name = 'authenticity_token';
        csrfInput.value = csrfToken;
        form.appendChild(csrfInput);
        
        document.body.appendChild(form);
        form.submit();
      }
    </script>
  </body>
</html>