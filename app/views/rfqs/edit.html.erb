<div class="max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit RFQ</h1>

  <div class="bg-white shadow rounded-lg p-6">
    <%= form_with(model: @rfq, local: true) do |form| %>
      <% if @rfq.errors.any? %>
        <div class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
          <h3 class="font-bold mb-2"><%= pluralize(@rfq.errors.count, "error") %> prohibited this RFQ from being saved:</h3>
          <ul class="list-disc list-inside">
            <% @rfq.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="space-y-4">
        <div>
          <%= form.label :title, class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :title, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <div>
          <%= form.label :description, class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_area :description, rows: 5, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <div>
          <%= form.label :deadline, class: "block text-sm font-medium text-gray-700" %>
          <%= form.datetime_field :deadline, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <div>
          <%= form.label :status, class: "block text-sm font-medium text-gray-700" %>
          <%= form.select :status, options_for_select([['Draft', 'draft'], ['Published', 'published'], ['Closed', 'closed']], @rfq.status), {}, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
        </div>

        <% if @rfq.documents.attached? %>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Current Attachments</label>
            <div class="space-y-2">
              <% @rfq.documents.each do |document| %>
                <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                  <div class="flex items-center space-x-3">
                    <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                    <div>
                      <p class="text-sm font-medium text-gray-900"><%= document.filename %></p>
                      <p class="text-xs text-gray-500"><%= number_to_human_size(document.byte_size) %></p>
                    </div>
                  </div>
                  <%= link_to "Download", document_path(document.signed_id, disposition: "attachment"), 
                      class: "text-sm text-blue-600 hover:text-blue-500" %>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>

        <div>
          <%= form.label :documents, "Add More Attachments", class: "block text-sm font-medium text-gray-700 mb-2" %>
          <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-md">
            <div class="space-y-1 text-center">
              <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48" aria-hidden="true">
                <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              <div class="flex text-sm text-gray-600">
                <%= form.file_field :documents, multiple: true, class: "sr-only", id: "file-upload-edit", data: { action: "change->file-upload#preview" } %>
                <label for="file-upload-edit" class="relative cursor-pointer bg-white rounded-md font-medium text-blue-600 hover:text-blue-500 focus-within:outline-none focus-within:ring-2 focus-within:ring-offset-2 focus-within:ring-blue-500">
                  <span>Upload files</span>
                </label>
                <p class="pl-1">or drag and drop</p>
              </div>
              <p class="text-xs text-gray-500">PDF, DOC, DOCX, XLS, XLSX up to 10MB each</p>
            </div>
          </div>
          <div id="file-list" class="mt-4"></div>
        </div>

        <div class="flex justify-end space-x-3 pt-4">
          <%= link_to "Cancel", @rfq, class: "px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50" %>
          <%= form.submit "Update RFQ", class: "px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700" %>
        </div>
      </div>
    <% end %>
  </div>
</div>